#!/bin/bash
echo "Compiling $PYFILE ..."
python3 -c "import marshal; f=open('$PYFILE'); code=compile(f.read(), '$PYFILE', 'exec'); f.close(); open('${PYFILE}c', 'wb').write(b'\0\0\0\0' + marshal.dumps(code))"
