#!/bin/bash
echo "Compiling $PYFILE ..."
python3 -m compileall "$PYFILE" --invalidation-mode=unchecked-hash
compiled=$(find __pycache__ -name "$(basename "$PYFILE" .py).*pyc")
mv "$compiled" "${PYFILE}c"
